# Secrets — never commit
cf_token.txt
**/cf_token.txt
.dev.vars
**/.dev.vars
*.pem
*.key

# Dependencies / build
node_modules/
**/node_modules/
.wrangler/
**/.wrangler/

# OS / editor
.DS_Store
Thumbs.db

# Local data exports / snapshots — never commit (contain lead PII)
snapshots/

# One-off data backfills / rollbacks — contain lead PII, keep local only.
# (Migrations under cloud/migrate_v*.sql ARE source and stay tracked.)
cloud/backfill_owners*.sql
cloud/rollback_*.sql
cloud/merge_master.sql
cloud/fix_dates.sql
cloud/import_videos.sql
cloud/backfill_country.sql
cloud/duplicate_conflicts.csv
unmapped_owners.txt

# Build artifact — rebuild with Compress-Archive from extension/
superprofile-crm-extension.zip
